# Face-detection
The aim of the task is to implement a logistic regression model with training algorithms for the task of face detection in the photo. 
Use Model training, Gradient algorithm, stochastic simple gradient algorithm, regularization, model selection IN content.py, operation correctness testing in test.py. 
